{
      # ---- SYSTEM SETTINGS ---- #
      systemSettings = {
        username = "marc";
        timezone = "America/Chicago"; # select timezone
        locale = "en_US.UTF-8"; # select locale
      };
}
